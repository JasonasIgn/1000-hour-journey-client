import { ComponentStyleConfig } from "@chakra-ui/react";

export const Popover: ComponentStyleConfig = {
  baseStyle: {},
  sizes: {
    sm: {
      content: {
        width: "200px",
      },
    },
  },
};

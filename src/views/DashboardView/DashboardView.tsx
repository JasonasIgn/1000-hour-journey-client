import { FC } from "react";
import { Container, Text } from "@chakra-ui/react";

export const DashboardView: FC = () => {
  return (
    <Container maxW="6xl">
      <Text>Dashboard</Text>
    </Container>
  );
};
